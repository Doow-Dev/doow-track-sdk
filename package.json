{
  "name": "doow-track-sdk",
  "version": "0.0.0",
  "private": true,
  "description": "Official Doow SDKs for usage telemetry",
  "workspaces": [
    "sdks/typescript",
    "sdks/react",
    "sdks/nextjs",
    "sdks/react-native"
  ],
  "scripts": {
    "prepare": "husky",
    "lint": "npm run lint --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Doow-Dev/doow-track-sdk.git"
  },
  "license": "MIT"
}
